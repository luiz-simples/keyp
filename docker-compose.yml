services:
  redis:
    image: redis:alpine
    container_name: keyp-redis-test
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - keyp-network

volumes:
  redis_data:

networks:
  keyp-network:
    driver: bridge
